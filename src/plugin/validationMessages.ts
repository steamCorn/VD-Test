import IValidationMessages from '../interfaces/IValidationMessages';

const validationMessages: IValidationMessages = {
  messages: {
    required: 'Dit veld is verplicht.',
  },
};

export default validationMessages;
